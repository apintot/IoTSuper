<UserControl x:Class="IoTSuper_DesktopApp.Controladores.Componentes.DisplayUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IoTSuper_DesktopApp.Controladores.Componentes"
             mc:Ignorable="d" Width="200">
    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="BlueRoundedTextBox">
            <Setter Property="Background" Value="#E6F0FA"/>
            <Setter Property="BorderBrush" Value="#5A9BD8"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="115"/>
            <Setter Property="Text" Value="najniebezpieczniejszy"/>
            <Setter Property="Foreground" Value="#1C3A5F"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0"
                                          x:Name="PART_ContentHost"
                                          VerticalScrollBarVisibility="Hidden"
                                          HorizontalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Cuando el TextBox recibe foco -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2C6DB2"/>
                                <Setter Property="Background" Value="#D9EBFA"/>
                            </Trigger>
                            <!-- Cuando está deshabilitado -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F0F0F0"/>
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox" x:Key="BlueRoundedCheckBox">
            <Setter Property="Foreground" Value="#1C3A5F"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Caja -->
                            <Border x:Name="Border"
                                    Width="20" Height="20"
                                    CornerRadius="5"
                                    BorderBrush="#5A9BD8"
                                    BorderThickness="2"
                                    Background="#E6F0FA"
                                    VerticalAlignment="Center"
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M0,5 L3,8 L8,0"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>

                            <!-- Texto -->
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Triggers -->
                        <ControlTemplate.Triggers>
                            <!-- Marcado -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2C6DB2"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2C6DB2"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#D9EBFA"/>
                            </Trigger>

                            <!-- Deshabilitado -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter TargetName="Border" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="mgrdMainLCD" MouseLeftButtonUp="mgrdMainLCD_MouseLeftButtonUp" MouseEnter="mgrdMainLCD_MouseEnter" MouseLeave="mgrdMainLCD_MouseLeave">
        <Grid x:Name="stckLCD">
            <Grid.RowDefinitions>
                <!-- Fila fija arriba -->
                <RowDefinition Height="Auto"/>
                <!-- Fila que se expande con el contenido -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="grdPrimaryLCD" Grid.Row="0" Height="100">
                <Rectangle HorizontalAlignment="Center" VerticalAlignment="Center" Fill="AliceBlue" Height="60" Width="98"/>
                <Image x:Name="imgLCD" Source="{StaticResource ILcd}" RenderOptions.BitmapScalingMode="HighQuality" Stretch="Uniform" />
                <Image x:Name="imgEstado" Source="{StaticResource IRojo}" RenderOptions.BitmapScalingMode="HighQuality" Margin="36,60,124,0"/>
                <TextBlock x:Name="txbLCD" Background="AliceBlue" Text="NULL" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Top" TextAlignment="Center" FontFamily="Agency FB" FontWeight="ExtraBold"/>
            </Grid>
            <Grid Grid.Row="1" Background="White" Height="380" Visibility="Visible" x:Name="grdSecundaryLCD">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Nombre :" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbNombre" Style="{StaticResource BlueRoundedTextBox}" MaxLength="30" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Topic:" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbTopic" Style="{StaticResource BlueRoundedTextBox}" MaxLength="20" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Fila 1:" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbFila1" Style="{StaticResource BlueRoundedTextBox}" MaxLength="20" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Fila 2:" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbFila2" Style="{StaticResource BlueRoundedTextBox}" MaxLength="20" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="4" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Fila 3:" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbFila3" Style="{StaticResource BlueRoundedTextBox}" MaxLength="20" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="5" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Fila 4:" FontSize="15" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBox x:Name="txbFila4" Style="{StaticResource BlueRoundedTextBox}" MaxLength="20" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="6" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Numero de vistas: " FontSize="20" HorizontalAlignment="Center" TextAlignment="Center"/>
                    <TextBlock x:Name="txbVistas" Text="0" FontSize="20" HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="7" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image Source="{StaticResource IBack}" RenderOptions.BitmapScalingMode="HighQuality" Height="30" Margin="0,0,25,0" MouseLeftButtonUp="Volver_MouseLeftButtonUp"/>
                    <Image x:Name="imgBorrar" Source="{StaticResource IBasked}"  RenderOptions.BitmapScalingMode="HighQuality" Height="30" MouseLeftButtonUp="Basura_MouseLeftButtonUpAsync"/>
                    <Image Source="{StaticResource ISave}" RenderOptions.BitmapScalingMode="HighQuality" Margin="25,0,0,0" Height="30" MouseLeftButtonUp="Save_MouseLeftButtonUpAsync"/>
                </StackPanel>

            </Grid>
        </Grid>
    </Grid>
</UserControl>
